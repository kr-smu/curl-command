#include <kr2_program_api/api_v1/program_interface.h>
#include <curl_command/curl_command.h>
/*
int main(int argc, const char * argv[]) {

    // Initialize API.
    auto api = kr2_program_api::ProgramInterface::factorySingletonInterface({});

    // Create instance of your command class. 
    auto *command = new kswx_template::MyCommand(api.get());
    
    // Test your command.
    auto target = kr2_program_api::Number();
    command->setValue(target, 0.5);

    return 0;
}
*/